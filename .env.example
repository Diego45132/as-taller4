# Este archivo contiene las variables de entorno necesarias para la configuración
# de los servicios y bases de datos del proyecto.
# Renombra este archivo a ".env" y ajusta los valores según sea necesario.

# CONFIGURACIÓN GENERAL

# URL del API Gateway. El frontend se conectará a esta dirección.
# En un entorno de Docker Compose, el nombre del servicio es el hostname.
API_GATEWAY_URL=http://api-gateway:8000

# VARIABLES PARA EL SERVICIO DE AUTENTICACIÓN

# URL del servicio de autenticación (FastAPI)
AUTH_SERVICE_URL=http://auth-service:8001
# URL de la base de datos de autenticación (MongoDB)
AUTH_DATABASE_URL=mongodb://auth-db:27017/auth_db

# VARIABLES PARA LOS MICROSERVICIOS (ajusta nombres y puertos)

# Servicio 1
SERVICE1_URL=http://service1-service:8002
SERVICE1_DATABASE_URL=postgresql://user:password@service1-db:5432/service1_db

# Servicio 2
SERVICE2_URL=http://service2-service:8003
SERVICE2_DATABASE_URL=postgresql://user:password@service2-db:5432/service2_db

# Servicio 3
SERVICE3_URL=http://service3-service:8004
SERVICE3_DATABASE_URL=postgresql://user:password@service3-db:5432/service3_db

# VARIABLES ADICIONALES (ejemplos)

# Redis URL para cache o sesiones compartidas
REDIS_URL=redis://redis-db:6379/0

# Variables para JWT o seguridad (ejemplo)
JWT_SECRET_KEY=your_secret_key_here
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
